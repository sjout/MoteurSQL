SELECT nom, age, op.numero, designationM
FROM operateur op, qualif_sur qs, machine m, piece p
WHERE  qs.numero = op.numero
AND    op.numero = p.numero
AND    p.referenceM = m.referenceM
AND    m.designationM = "Tour numerique"
;
